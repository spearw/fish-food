# Benched cards (not in lightning_pack.tres — on purpose)

- `amplification_upgrade.tres` (+8–20% all damage)
- `overcharge_upgrade.tres` (+crit chance/damage)
- `reach_upgrade.tres` (+12–30% area)

The core-deck dissolution LANDED (design doc §1b, July 2026): decks now carry the SHARED stat
cards (`upgrades/core/player_*.tres` — lightning has damage/crit×2/luck/max-health). These three
stay benched as the ready-made template for the NEXT escalation: deck-flavored stat VARIANTS
(different values/names per deck), a balance lever we haven't needed yet. Don't re-add them
alongside the shared cards — they'd duplicate stats in the same deck.
