# Benched cards (not in lightning_pack.tres — on purpose)

- `amplification_upgrade.tres` (+8–20% all damage)
- `overcharge_upgrade.tres` (+crit chance/damage)
- `reach_upgrade.tres` (+12–30% area)

These duplicate core-deck stats. Benched pending a design decision: possibly dissolving
the core deck and distributing generic stat cards across themed decks (with deliberate
overlap, so pairing two decks that both carry e.g. damage lets you double down on it).
If that lands, these three are the lightning deck's stat package — re-add them to the
pack instead of authoring new ones. See docs/deck_and_synergy_design.md.
